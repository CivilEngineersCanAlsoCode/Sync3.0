# LaTeX Archive

**Reason archived:** Decision made on 2026-03-04 to use **HTML + CSS** as the default resume output format.

LaTeX requires manual compilation via Overleaf or `pdflatex` — HTML + CSS can be printed directly to PDF from any browser with full brand color support via CSS variables.

## Archived files

- `Satvik_Jain_Resume.tex` — Full resume content in LaTeX (use as content reference)
- `CV Format.tex` — Original blank LaTeX template
- `resume_branding_cheatsheet.csv` — Company hex color codes (still valid for CSS variables)

## If you ever want to switch back

1. Move files back to `Input/` and `Templates/`
2. Update `resume_customization_plan.md` Phase 5.5 and Phase 6 to re-enable LaTeX option
3. Update bd task PM-151 description
